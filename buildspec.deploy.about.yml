version: 0.2

cache:
  paths:
    - node_modules/**/*

env:
  shell: bash

phases:
    install:
      runtime-versions:
        nodejs: 16
      on-failure: ABORT
      commands:
        - |
          npm install
    build:
      on-failure: ABORT
      commands: 
        - |
          npm run wefwefwef
    post_build:
      on-failure: ABORT
      commands:
        - |
          aws s3 cp dist s3://${DEPLOY_S3}  --recursive 
          aws cloudfront create-invalidation --distribution-id E2H7PZWLM92D0F --paths "/about/index.html"
